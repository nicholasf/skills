[project]
name = "ask-foreign-agent"
version = "0.1.0"
description = "qwen3-coder on pond as an interactive agent in the Claude Code session"
requires-python = ">=3.11"
dependencies = [
    "langchain-core>=0.3",
    "langchain-openai>=0.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
]

[tool.uv]
package = false
